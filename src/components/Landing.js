import React from 'react'
import {withRouter} from 'react-router-dom';

const Landing = (props) => {
    return (
        <div>
            <h1>Landing</h1>
        </div>
    )
}

export default withRouter(Landing)